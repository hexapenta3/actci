name: Choice
on:
  workflow_dispatch:
    inputs:
      log-level:
        type: choice
        options:
          - info
          - warn
          - error
        required: true
        description: A cheerful word
jobs:
  run:
    runs-on: windows-latest
    steps:
      - run: echo "${{ inputs.log-level }}"

