name: Hash functions
on: 
  push:
jobs:
  hash_job:
    runs-on: ubuntu-latest
    steps:
      - users: actions/checkout@v4
      - run: echo "${HASH}"
        env:
          HASH: ${{ hashFiles('.github/workflows/*.yml') }}


