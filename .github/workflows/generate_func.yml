name: Env_Type
on: 
  push:
jobs:
  env_job:
    runs-on: ubuntu-latest
    steps:
      - id: source
        run: echo "result=Hello" >> ${GITHUB_OUTPUT}
      - env:
          RESULT: ${{ steps.source.outputs.result }}
        run: echo "${RESULT}"



