name: Hash functions
on: 
  push:
jobs:
  hash_job:
    runs-on: ubuntu-latest
    env:
      RUNID: ${{ github.run_id }}
    steps:
      - uses: actions/checkout@v4
      - run: echo "${HASH}"
        env:
          HASH: ${{ hashFiles('.github/workflows/*.yml') }}
          RUNID: ${{ github.run_id }}
      - run: echo "Hello"
        if: ${{ contains(env.RUNID, '1') }}
      - run: echo "${RUNID}"


